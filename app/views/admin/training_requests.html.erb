<% content_for :title, "Training Requests - Admin" %>

<% extra_html = capture do %>
  <select name="status" class="border rounded px-2 py-1">
    <option value="">All</option>
    <% TrainingRequest.distinct.pluck(:status).compact.each do |s| %>
      <option value="<%= s %>" <%= 'selected' if params[:status] == s %>><%= s %></option>
    <% end %>
  </select>
  <button type="submit" style="background-color:#5B9EA6;color:#fff;" class="px-3 py-1 rounded">Filter</button>
<% end %>

<%= render 'admin/search_form', placeholder: 'Search training requests...', width: 'w-64', show_button: true, extra_html: extra_html %>

<%= pagy_tailwind_nav(@pagy) if defined?(@pagy) %>

<div class="bg-white rounded-lg shadow max-h-[calc(100vh-14rem)] overflow-auto">
  <table class="min-w-full">
    <thead class="bg-gray-50">
      <tr>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">ID</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Topic</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Description</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Votes</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Created</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-gray-200">
      <% @training_requests.each do |request| %>
        <tr class="hover:bg-gray-50">
          <td class="px-6 py-4"><%= request.id %></td>
          <td class="px-6 py-4"><%= truncate(request.topic, length: 30) %></td>
          <td class="px-6 py-4"><%= truncate(request.description, length: 40) %></td>
          <td class="px-6 py-4"><%= request.votes %></td>
          <td class="px-6 py-4"><%= request.created_at.strftime("%Y-%m-%d") %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<%= pagy_tailwind_nav(@pagy) if defined?(@pagy) %>
